# spec.md — moved

The `nhs-number` specification now lives in the [`spec/`](spec/) directory,
split one file per section. Start at [`spec/index.md`](spec/index.md).

Section numbering is unchanged: a reference such as `§6.4` now resolves via
the table of contents in `spec/index.md` (§6 is
[spec/06-check-digit-algorithm.md](spec/06-check-digit-algorithm.md)).
